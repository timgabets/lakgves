language: rust
dist: bionic
addons:
  apt:
    packages:
    - rpm
rust:
  - stable
jobs:
  fast_finish: true
cache: cargo
install:
  - cargo install cargo-rpm
script:
  - cargo test --verbose --all
after_success:
  - cargo rpm build
